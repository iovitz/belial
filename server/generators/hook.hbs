/**
 * {{name}} hook
 *
 * @description :: implement the description here
 */

module.exports = function defineLimiterHook(sails) {
  // ...

  return {

    /**
     * Runs when this Sails app loads/lifts.
     */
    async initialize() {

      const cost = Number(process.hrtime.bigint() - startTime).toLocaleString()
      LoggerService.bootstrap(`Initializing custom hook (\`{{name}}\`), cost: ${cost}`)
    },

  }
}
